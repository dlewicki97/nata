function fillTagsAfterRenderListing() {
  // let activeListItems = document.querySelectorAll(
  //   "ul.filter-list li:not(.show-filters).active"
  // );
  // let tags = document.querySelectorAll(".used-filters .tag");
  // for (let i = 0; i < activeListItems.length; i++) {
  //   if (
  //     [...tags].find(
  //       (tag) => activeListItems[i].id == tag.id.replace("tag-", "")
  //     ) ??
  //     true
  //   ) {
  //     document.querySelector(
  //       ".used-filters .tags"
  //     ).innerHTML += `<div id="tag-${activeListItems[i].id}" class="tag">${activeListItems[i].innerText}
  //       <span class="dismiss">x</span>
  //   </div>`;
  //   }
  // }
}
